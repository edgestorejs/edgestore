---
'@edgestore/server': minor
'@edgestore/react': minor
'@edgestore/shared': minor
---

Support automatic S3 multipart uploads with scoped sessions and cancellation, backend uploads, private signed downloads, stable key references, and configurable object settings. Send provider-specific browser upload headers and stop queued multipart transfers when canceled. Reject unsupported S3 temporary/replacement and cookie-based access-control configurations instead of silently ignoring them, and document setup and remaining limitations.
