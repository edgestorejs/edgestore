---
'@edgestore/sdk': minor
'@edgestore/server': patch
---

Allow the EdgeStore provider to pass router bucket settings to managed uploads, so the first backend upload can create a bucket with the correct visibility.
