---
'@edgestore/cli': patch
---

Require explicit application selection at unlinked monorepo roots, even when root tooling declares a framework. Install both server and React packages for combined Hono/Vite React applications.
