# EdgeStore TanStack Start example

This example uses the current Vite-based TanStack Start architecture with a
Nitro Node.js server.

```bash
pnpm --filter start-basic dev
```

The EdgeStore API route lives at `src/routes/api/edgestore.$.ts`, and the upload
UI lives at `src/routes/index.tsx`.
